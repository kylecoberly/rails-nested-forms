<% if @artist.errors.any? %>
  <ul class="errors">
    <%= @artist.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>

<%= form_for @artist do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>

  <%= f.submit "Add Artist" %>
<% end %>
